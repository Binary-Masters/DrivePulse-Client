
const Button = ({buttonName}) => {
    return (
        <button className="text-xl bg-blue-600 px-4 py-2 rounded-md text-black font-semibold">
            {buttonName}
        </button>
            
    );
};

export default Button;